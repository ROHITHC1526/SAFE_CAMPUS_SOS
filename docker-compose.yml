version: '3.8'

services:
  mongodb:
    image: mongo:7.0
    container_name: safecampus-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodata:/data/db
    restart: unless-stopped

volumes:
  mongodata:
